// Goal amount

// Current saved amount

// Target date

// Progress tracker (e.g., % or visual bar)

import { View, Text } from 'react-native';

export default function Goals() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Goals Screen</Text>
    </View>
  );
}
